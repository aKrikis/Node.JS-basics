let num1 = 10;
let = num2 = 3;

let divResult = num1 / num2;
let expr = (nu2 / num1) * (8.5 / 3.3);

let fixedDiv = (num1 / num2).toFixed(1); //Tha vgei me ena dekadiko
let inResult = Math.floor(num1 / num2); //returns int -- round down
let upperInt = Math.ceil(num1 / num2); //returns int -- upper round
let roundResult = Math.round(num1 / num2); //round up and down .5 

let modeResult = num1 % num2; //returns the modulo -- diaturei to proshmo (+ -)

console.log(divResult, expr, fixedDiv, inResult, upperInt, roundResult, modeResult);
