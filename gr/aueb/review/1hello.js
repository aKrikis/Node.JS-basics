/**
 * JS is dynamic, no datatype needed
 * use camelCase
 * case-sensitive
 * Avoid declaretion with var
 * take care of syntax
 */
const PI = 3.14;
const maxThreads = 150;
let counter = 0;
counter++;

console.log(PI, typeof PI, maxThreads, counter);
